<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.mapper.UsersMapper">
    <insert id="insertUser" parameterType="com.app.entity.Users">
        INSERT INTO users (user_id, password, real_name, sex, identity_card, birthday, user_type)
        VALUES (#{userId}, #{password}, #{realName}, #{sex}, #{identityCard}, #{birthday}, #{userType})
    </insert>
</mapper>
